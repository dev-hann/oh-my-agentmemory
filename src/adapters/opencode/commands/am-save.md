Save the following to long-term memory: $ARGUMENTS

Call `agentmemory_memory_save` with:
- content: the text after this command
- concepts: 2-5 comma-separated keywords you extract from it
- type: pick the closest match (pattern / preference / architecture / bug / workflow / fact)

After saving, confirm with a one-line summary of what was stored and the memory id. Do not claim success without the actual tool call.
