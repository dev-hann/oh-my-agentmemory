Search past agentmemory observations and lessons for: $ARGUMENTS

Call `agentmemory_memory_recall` with the user's query, then summarize what you found. Prefer `agentmemory_memory_smart_search` if the query is fuzzy or conceptual. Skip pinned slots — they are already in your context.

If nothing relevant, say so plainly. Do not fabricate recall results.
